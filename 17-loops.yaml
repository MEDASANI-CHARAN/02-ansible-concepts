- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello, {{ item }}" # Here is item called reserved key word
    loop:
    - Suresh
    - Ramesh
    - Joseph
    - Raheem